name: Stay Online Bot

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Clean Install
        run: |
          rm -rf node_modules package-lock.json
          npm install

      - name: Run the bot
        env:
          TOKEN: ${{ secrets.TOKEN }}
        run: node bot.js || echo "السكربت واجه مشكلة في تسجيل الدخول"
